# 把一个有效的镜头，变成别人也能用的 Prompt

感谢你分享实际制作经验。贡献不需要完整工作流：一个清楚的视觉任务、一段可复制指令和可信演示，就能帮助其他创作者。

## 提交什么

1. **问题**：原来的镜头哪里难理解？这项效果帮助观众看懂什么？
2. **Prompt**：中文自然语言，明确必填与可选输入、时间、素材和可编辑项。
3. **示例**：MP4；有声音设计时必须保留音频。可另提供轻量 GIF。
4. **范围**：本地渲染、ChatCut 时间线、目标画幅与版本，分别写清。
5. **来源**：原创或外部参考链接，素材许可与署名；不要提交无权分发的录屏、音乐或人物素材。

已有机制的画幅、样式和文案变化优先作为原 Prompt 变体；只有观看任务或动作机制发生变化，才建议新编号。

## 在哪里修改

- 公共指令与使用条件：`references/prompt-*.md`。
- 按用途入口：`PROMPT-LIBRARY.md`；首页只选少量代表效果。
- 原创本地示例：`assets/prompt-examples/`，可复现源文件放 `source/`。
- 真实 ChatCut 验证后才加入 `recipes/`，遵循现有 schema；本地渲染不要标 `verified`。
- 不需要改变所有用户的剪辑流程来贡献一个局部效果。

## 自查

```sh
python scripts/validate_talkdirector.py
python -m unittest discover -s tests
python scripts/check_local_links.py
```

修改演示时，完整解码并检查入场、可读保持、退场和退出后一帧；有声音就试听。源代码通过、成功渲染、看到合成帧、听过音频是不同证据，不互相代替。

PR 描述请写清：解决什么、与已有素材的差异、如何验证、仍有哪些限制。保留现有[授权](LICENSE)和[第三方声明](THIRD_PARTY_NOTICES.md)。
